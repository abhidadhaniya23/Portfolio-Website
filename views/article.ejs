<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%=title%>
    </title>
    <link rel="stylesheet" href="prism/prism.css">
    <link rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@5.15.3/css/fontawesome.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap');

        * {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
        }

        @font-face {
            font-family: CascadiaMono;
            src: url('font/CascadiaMono.woff2');
        }

        ::selection {
            color: white;
            background: var(--primary-color);
        }

        :root {
            --code-font: CascadiaMono, sans-serif;
            --primary-color: #0af;
            --light-primary-color: rgba(125, 212, 255, 0.5);
            --dark-primary: rgb(0, 151, 226);
            --hover-primary: rgb(75, 195, 255);
            /* --text-color: rgb(4, 6, 8); */
            --text-color: #000000ba;
        }

        body {
            background: rgb(251, 251, 251);
        }

        .main-body {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        img {
            display: flex;
            max-height: 20rem;
            max-width: 90vw;
            border-radius: 10px;
            text-align: center;
            /* margin: 2rem auto; */
        }

        li p {
            line-height: 0rem;
        }

        li {
            margin: .5rem 0;
        }

        p {
            color: var(--text-color);
            line-height: 2rem;
            font-size: 1.27rem;
        }

        h1 {
            color: var(--dark-primary);
            text-align: center;
        }

        h1 {
            font-size: 2.7rem;
        }

        h2 {
            font-size: 2.5rem;
        }

        h3 {
            font-size: 2.3rem;
        }

        h4 {
            font-size: 1.7rem;
        }

        h5 {
            font-size: 1.6rem;
        }

        h6 {
            font-size: 1.5rem;
        }

        h2,
        h3,
        h4,
        h5,
        h6 {
            color: var(--dark-primary);
            font-weight: 600;
            margin: 2rem 0 -.9rem 0;
        }

        hr {
            display: none;
        }

        a {
            font-weight: 500;
            color: var(--primary-color);
            text-decoration: none;
            transition: 0.3s ease;
            padding: .2rem .4rem;
            border-radius: 4px;
            position: relative;
            z-index: 1;
        }

        a::before {
            position: absolute;
            content: '';
            height: 2px;
            bottom: 4px;
            margin: 0 auto;
            padding: 0 5px;
            left: 0;
            right: 0;
            width: 90%;
            background: var(--light-primary-color);
            transition: 0.3s ease;
            z-index: -1;
        }

        a:hover::before {
            bottom: 2px;
            height: 25px;
            /* width: 100%; */
        }

        a:hover {
            color: var(--dark-primary);
        }

        .content {
            max-width: 65rem;
            padding: 3rem 5rem 5rem;
            margin: 0 40px;
            box-shadow: 0 4px 24px 7px rgb(0 0 0 / 4%), 0 12px 64px 1px rgb(0 0 0 / 10%);
            /* box-shadow: 0 8px 60px 0 rgb(0 0 0 / 11%), 0 12px 90px 0 rgb(0 0 0 / 11%); */
        }

        code {
            padding: 1px 3px;
            background: rgb(230, 230, 230);
            color: rgb(39, 39, 39);
            border-radius: 3px;
        }

        pre code {
            background: none;
        }

        pre,
        code,
        .token {
            font-family: var(--code-font) !important;
        }

        blockquote {
            background-color: rgb(219, 244, 255) !important;
            margin: 2.4rem auto;
            border-radius: 5px;
            border-left: .4rem solid var(--primary-color);
            padding: .15rem 1.2rem;
            font-weight: 500;
        }

        blockquote p {
            color: #666666;
        }

        li {
            font-size: 1.2rem;
            color: var(--text-color);
            list-style-type: disk;
            line-height: 2rem;
        }

        h1+div.code-toolbar,
        h2+div.code-toolbar,
        h3+div.code-toolbar,
        h4+div.code-toolbar,
        h5+div.code-toolbar,
        h6+div.code-toolbar {
            margin-top: 1.3rem;
        }

        pre {
            background: whitesmoke;
            padding: 2rem;
            font-size: 18px !important;
            border-radius: .3rem;
            border-left: .3rem solid var(--primary-color);
        }

        .toolbar-item button {
            color: white !important;
            background: transparent !important;
            box-shadow: none !important;
        }

        .toolbar-item button span {
            box-shadow: 0px 0px 11px 1px rgb(212, 212, 212) !important;
            cursor: pointer !important;
            background: #00d0ff !important;
            color: whitesmoke !important;
            padding: 7px 20px !important;
            transition: .5s ease;
            font-family: 'Inter', sans-serif;
        }

        .toolbar-item button span:hover {
            background: #00b2da !important;
        }

        .coverImg {
            margin: 2rem auto;
        }

        .flex-row-class {
            display: flex;
            flex-direction: row;
            justify-content: center;
            align-items: center;
        }

        @media (max-width: 420px) {
            .content {
                padding: .5rem .25rem;
            }
        }
    </style>
</head>

<body>
    <div class="main-body">
        <div class="content">
            <h1 class="text-3xl">
                <%=title%>
            </h1>
            <div class="flex-row-class">
                <p><i class="fa fa-clock mr-2"></i>
                    <%=readingTime%> min. to read ,
                </p>
                <p>
                    <%=createdTime%> ago
                </p>
            </div>
            <img class="coverImg" src="<%=coverImg%>" alt="<%=title%>">
            <%- include('../partials/postContent.ejs')%>
        </div>
    </div>
    <!-- <div class="mode">
        <img src="">
    </div> -->

    <script src="prism/prism.js"></script>

    <script>
        const anchorTags = document.querySelectorAll('a');
        anchorTags.forEach(Element => Element.setAttribute("target", "_blank"))
    </script>

</body>

</html>